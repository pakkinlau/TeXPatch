Add Chrome Web Store screenshots here.

Suggested shots (PNG, 1280x800 or similar):
- 1.png: Popup with a selected text snippet, showing “Convert selection → clipboard”
- 2.png: Replacement in a textarea (toggle on)
- 3.png: Profile dropdown (katex/pandoc/github)

Keep file sizes small; avoid sensitive content. The Publish workflow will include this directory only via `scripts/pack-extension.sh` if you copy them under `packages/extension/assets/` instead. Store assets are optional for local testing.

